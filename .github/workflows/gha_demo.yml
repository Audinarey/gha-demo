name: Github Action Demo
on: push
jobs:
  action-demo:
    runs-on: ubuntu-latest
    steps:
      - name: checkout the code
        uses: actions/checkout@v7
      - name: print the index.html content
        run: cat index.html
